import { Button } from "@mui/material";

export default function Test() {
    return (
      <div className="flex flex-col items-center justify-center w-screen h-screen">
        <Button variant="contained">Test OK</Button>
      </div>
    )
}